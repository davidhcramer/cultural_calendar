{
  "name": "cultural-calendar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader native --host 127.0.0.1",
    "build": "vite build --configLoader native && node scripts/precache.mjs",
    "preview": "vite preview --configLoader native --host 127.0.0.1",
    "test": "node --test --test-isolation=none tests/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.110.0"
  },
  "devDependencies": {
    "jsdom": "26.1.0",
    "vite": "8.1.0"
  }
}
